# CS200 Demo Sandbox

This fork is used to experiment with **various GitHub features**.

- Actions
- PRs
- Basic features

---   

To use the Dockerfile:

```
docker build -t sandbox .
docker run -it --rm sandbox
```